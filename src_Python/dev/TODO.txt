2D PIV BOS


TODO
----

- In `main.py`, implement "export to disk", like as in `live_preview.py`

- Expand config.ini with desired camera resolution and exposure time (Ximea)

- Examine the GIL lock and release
  https://www.maartenbreddels.com/perf/jupyter/python/tracing/gil/2021/01/14/Tracing-the-Python-GIL.html
  viztracer -o output.html --ignore_frozen -m main

- Try Cupy


FINISHED TODO
-------------

- Add check on the `IWs_slices` calculation in `main.py` and fix super division by possibly too many workers, resulting in duplicate single item slices.
DONE

- Collect all correlation maps `C` into pre-allocated matrix for later sub-pixel fitting.
DONE, speed improved

- Sub-pixel fitting only makes sense at last `iIW_size` step (lowest IW size).
DONE, speed improved

- Sub-pixel fitting can run fully jitted and parallel
DONE, speed improved

- DITCH ALL list append. Turn them into a pre-allocated list of numpy arrays where we will write directly into.
DONE, speed improved

- Flip complete second image left-to-right and up-to-down (fliplrud) ahead of time. Then take subsets out of this.
DONE, speed improved

- Fix incorrect [-2] indexing with (stage_idx - 1)
DONE

- In 2D-convolve, try the passed array length, instead of 'fastlen'
DONE. Outcome: zero-padding to fastlen is needed for correct results. Without zero-padding the results suffer from edge defects, though it is faster.

- Linearize matrices. Not longer (IW_idx_x, IW_idx_y) but step through using (IW_idx)
DONE, speed improved

- Examine possible speed up of `convolve2d()`. Timeit.
DONE, speed improved

- `if fast_max(IW_A_) <= 0` can be changed to exit early.
DONE, speed improved

- Parallelize by dividing pool of all IWs into chunks to be processed in separate parallel threads
DONE, speed improved

- Fix the terminal debug output. Turn into a function being called at the end of the script.
DONE, minor speed improvement

- Change `ThreadPoolExecutor` to `ProcessPoolExecutor` and check for possible performance increase. Requires shared memory pool.
  https://mingze-gao.com/posts/python-shared-memory-in-multiprocessing/
  https://santhalakshminarayana.github.io/blog/super-fast-python-multi-processing
DON'T, threads are working just fine reaching efficient 100% CPU load

- Use OpenCV instead of skimage.io for image processing
DONE
