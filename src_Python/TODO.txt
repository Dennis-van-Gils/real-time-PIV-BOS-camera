2D PIV BOS

TODO
----

x 1) Collect all correlation maps `C` into pre-allocated matrix for later sub-pixel fitting.
DONE, speed improved

x 2) Sub-pixel fitting only makes sense at last `iIW_size` step (lowest IW size).
DONE, speed improved

x 3) Sub-pixel fitting can now run fully jitted and parallel
DONE, speed improved

x 4) DITCH ALL list append. Turn them into a pre-allocated list of numpy arrays where we will write directly into.
DONE, speed improved

x 5) Flip complete second image left-to-right and up-to-down (fliplrud) ahead of time. Then take subsets out of this.
DONE

x 6) Fix incorrect [-2] indexing with (stage_idx - 1)
DONE

7) In 2D-convolve, try the passed array length, instead of 'fastlen'

8) Try Cupy

x 9) Linearize matrices. Not longer (IW_idx_x, IW_idx_y) but step through using (IW_idx)
DONE, speed improved

x 10) Fix the terminal debug output. Perhaps as a function being called at the end of the script.
DONE

11) Try OpenCV instead of skimage.io for image processing