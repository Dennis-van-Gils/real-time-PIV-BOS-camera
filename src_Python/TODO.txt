2D PIV BOS

TODO
----

8) Try Cupy

Each stage should cpu<->gpu exchange of C_maps. CPU needs to process C_maps into vector displacement. (Perhaps could be actually left to the gpu entirely.)

CPU: Copy over full frames A and B into GPU.
CPU: Prepare grid limits and centers for all stages. Copy those to GPU
GPU: (Write a kernel)
  1) Background subtraction A * B
  2) A_ = fliplrud(A)
  3) Walk over all stages
     3a) Stage 0 is special.
         Walk over all IWs
           select IW_A_ and IW_B out of A and B
           Perform 2D xcros
           Store C_maps
         Return C_maps as np.array
     3b) Stage N > 0
         Walk over al IWs
           Use a preshift window selection
           Check for LRUD borders
           select IW_A_ and IW_B out of A and B
           Perform 2D xcros
           Store C_maps
        Return C_maps as np.array
CPU: Process C_maps into vector displacement


11) Try OpenCV instead of skimage.io for image processing

